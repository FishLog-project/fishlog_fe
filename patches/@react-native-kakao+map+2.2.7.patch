diff --git a/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapPackage.kt b/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapPackage.kt
index c3a003d..d9ab736 100644
--- a/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapPackage.kt
+++ b/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapPackage.kt
@@ -5,8 +5,12 @@ import com.facebook.react.bridge.NativeModule
 import com.facebook.react.bridge.ReactApplicationContext
 import com.facebook.react.module.model.ReactModuleInfo
 import com.facebook.react.module.model.ReactModuleInfoProvider
+import com.facebook.react.uimanager.ViewManager
 
 class RNCKakaoMapPackage : TurboReactPackage() {
+  override fun createViewManagers(reactContext: ReactApplicationContext): List<ViewManager<*, *>> =
+    listOf(RNCKakaoMapViewManager())
+
   override fun getModule(
     name: String,
     reactContext: ReactApplicationContext,
diff --git a/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapView.kt b/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapView.kt
new file mode 100644
index 0000000..dfc2a6f
--- /dev/null
+++ b/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapView.kt
@@ -0,0 +1,278 @@
+﻿package net.mjstudio.rnkakao.map
+
+import android.graphics.Bitmap
+import android.graphics.Canvas
+import android.graphics.Color
+import android.graphics.Paint
+import android.graphics.Path
+import android.widget.FrameLayout
+import com.facebook.react.bridge.LifecycleEventListener
+import com.facebook.react.bridge.Arguments
+import com.facebook.react.bridge.WritableMap
+import com.facebook.react.uimanager.ThemedReactContext
+import com.facebook.react.uimanager.UIManagerHelper
+import com.facebook.react.uimanager.events.Event
+import com.kakao.vectormap.KakaoMap
+import com.kakao.vectormap.KakaoMapReadyCallback
+import com.kakao.vectormap.LatLng
+import com.kakao.vectormap.MapLifeCycleCallback
+import com.kakao.vectormap.MapType
+import com.kakao.vectormap.MapView
+import com.kakao.vectormap.PoiScale
+import com.kakao.vectormap.camera.CameraAnimation
+import com.kakao.vectormap.camera.CameraUpdateFactory
+import com.kakao.vectormap.label.Label
+import com.kakao.vectormap.label.LabelLayer
+import com.kakao.vectormap.label.LabelLayerOptions
+import com.kakao.vectormap.label.LabelOptions
+import com.kakao.vectormap.label.LabelStyle
+import com.kakao.vectormap.label.LabelStyles
+
+internal data class CameraProps(val latitude: Double, val longitude: Double, val zoomLevel: Int)
+
+internal data class LatLngProps(val latitude: Double, val longitude: Double)
+
+internal data class SpotProps(val id: Int, val latitude: Double, val longitude: Double)
+
+/** 마커를 눌렀을 때 JS 로 올려 보내는 이벤트. onSpotPress 로 도착한다. */
+internal class SpotPressEvent(surfaceId: Int, viewId: Int, private val spotId: Int) :
+  Event<SpotPressEvent>(surfaceId, viewId) {
+  override fun getEventName() = EVENT_NAME
+
+  override fun getEventData(): WritableMap = Arguments.createMap().apply { putInt("id", spotId) }
+
+  companion object {
+    const val EVENT_NAME = "topSpotPress"
+  }
+}
+
+internal class RNCKakaoMapView(private val reactContext: ThemedReactContext) : FrameLayout(reactContext), LifecycleEventListener {
+  private val nativeMapView = MapView(reactContext)
+  private var kakaoMap: KakaoMap? = null
+  private var camera: CameraProps? = null
+  private var currentLocation: LatLngProps? = null
+  private var currentLocationLabel: Label? = null
+  private var spots: List<SpotProps> = emptyList()
+  private var spotLayer: LabelLayer? = null
+  private var spotStyles: LabelStyles? = null
+  var cameraAnimationDuration = 0
+
+  var poiEnabled = true
+    set(value) { field = value; kakaoMap?.setPoiVisible(value) }
+  var poiClickable = true
+    set(value) { field = value; kakaoMap?.setPoiClickable(value) }
+  var language = "ko"
+    set(value) { field = value; kakaoMap?.setPoiLanguage(value) }
+  var poiScale = PoiScale.REGULAR
+    set(value) { field = value; kakaoMap?.setPoiScale(value) }
+  var mapType = MapType.NORMAL
+    set(value) { field = value; kakaoMap?.changeMapType(value) }
+  var buildingScale = 1f
+    set(value) { field = value; kakaoMap?.setBuildingHeightScale(value) }
+  var cameraMinLevel = 1
+    set(value) { field = value; kakaoMap?.setCameraMinLevel(value) }
+  var cameraMaxLevel = 20
+    set(value) { field = value; kakaoMap?.setCameraMaxLevel(value) }
+  var showScaleBar = false
+    set(value) { field = value; if (value) kakaoMap?.scaleBar?.show() else kakaoMap?.scaleBar?.hide() }
+  var showCompass = false
+    set(value) { field = value; if (value) kakaoMap?.compass?.show() else kakaoMap?.compass?.hide() }
+
+  init {
+    addView(nativeMapView, LayoutParams(LayoutParams.MATCH_PARENT, LayoutParams.MATCH_PARENT))
+    reactContext.addLifecycleEventListener(this)
+    nativeMapView.start(
+      object : MapLifeCycleCallback() {
+        override fun onMapDestroy() = Unit
+        override fun onMapError(error: Exception) = Unit
+      },
+      object : KakaoMapReadyCallback() {
+        override fun onMapReady(map: KakaoMap) {
+          kakaoMap = map
+          spotLayer = null
+          spotStyles = null
+          applyMapOptions(map)
+          camera?.let(::moveCamera)
+          map.setOnLabelClickListener { _, _, label ->
+            (label.tag as? Int)?.let(::emitSpotPress)
+          }
+          applyCurrentLocation()
+          applySpots()
+        }
+
+        override fun getPosition(): LatLng = camera?.let { LatLng.from(it.latitude, it.longitude) } ?: super.getPosition()
+        override fun getZoomLevel(): Int = camera?.zoomLevel ?: super.getZoomLevel()
+      },
+    )
+  }
+
+  fun setCamera(value: CameraProps) {
+    camera = value
+    moveCamera(value)
+  }
+
+  fun setCurrentLocation(value: LatLngProps?) {
+    currentLocation = value
+    applyCurrentLocation()
+  }
+
+  fun setSpots(value: List<SpotProps>) {
+    spots = value
+    applySpots()
+  }
+
+  /**
+   * 낚시 스팟 마커를 다시 그린다.
+   *
+   * 현재 위치와 레이어를 분리한다 — 목록이 바뀔 때마다 removeAll 로 지우는데,
+   * 같은 레이어를 쓰면 현재 위치 마커까지 함께 지워지기 때문이다.
+   * 각 라벨의 tag 에 스팟 id 를 넣어 두고, 클릭 때 그 값을 그대로 올려 보낸다.
+   */
+  private fun applySpots() {
+    val map = kakaoMap ?: return
+    val labelManager = map.labelManager ?: return
+
+    val layer =
+      spotLayer
+        ?: labelManager
+          .addLayer(LabelLayerOptions.from(SPOT_LAYER_ID).apply { clickable = true })
+          ?.also { spotLayer = it }
+        ?: return
+
+    layer.removeAll()
+    if (spots.isEmpty()) return
+
+    // 마커 아이콘은 목록이 바뀔 때마다 다시 그릴 필요가 없다 (검색 한 글자마다 호출된다)
+    val styles =
+      spotStyles
+        ?: labelManager
+          .addLabelStyles(
+            LabelStyles.from(SPOT_STYLE_ID, LabelStyle.from(createSpotPin()).setAnchorPoint(0.5f, 1f)),
+          )
+          ?.also { spotStyles = it }
+        ?: return
+
+    layer.addLabels(
+      spots.map { spot ->
+        LabelOptions
+          .from(LatLng.from(spot.latitude, spot.longitude))
+          .setStyles(styles)
+          .setClickable(true)
+          .setTag(spot.id)
+      },
+    )
+  }
+
+  private fun emitSpotPress(spotId: Int) {
+    val dispatcher = UIManagerHelper.getEventDispatcherForReactTag(reactContext, id) ?: return
+    dispatcher.dispatchEvent(SpotPressEvent(UIManagerHelper.getSurfaceId(reactContext), id, spotId))
+  }
+
+  /** 기본 마커 — 커스텀 디자인이 나오기 전까지 쓰는 물방울 핀이다. */
+  private fun createSpotPin(): Bitmap {
+    val density = resources.displayMetrics.density
+    val width = (22 * density).toInt().coerceAtLeast(1)
+    val height = (30 * density).toInt().coerceAtLeast(1)
+    val bitmap = Bitmap.createBitmap(width, height, Bitmap.Config.ARGB_8888)
+    val canvas = Canvas(bitmap)
+    val paint = Paint(Paint.ANTI_ALIAS_FLAG)
+    val centerX = width / 2f
+    val headRadius = 9f * density
+    val headCenterY = headRadius + 1f * density
+
+    paint.color = Color.rgb(0, 121, 202)
+    canvas.drawPath(
+      Path().apply {
+        moveTo(centerX, height.toFloat())
+        lineTo(centerX - 6f * density, headCenterY + headRadius * 0.6f)
+        lineTo(centerX + 6f * density, headCenterY + headRadius * 0.6f)
+        close()
+      },
+      paint,
+    )
+    canvas.drawCircle(centerX, headCenterY, headRadius, paint)
+    paint.color = Color.WHITE
+    canvas.drawCircle(centerX, headCenterY, headRadius - 3.5f * density, paint)
+
+    return bitmap
+  }
+
+  /** 현재 위치 라벨을 지도에 올린다. 이미 올라와 있으면 위치만 옮긴다. */
+  private fun applyCurrentLocation() {
+    val map = kakaoMap ?: return
+    val position = currentLocation
+
+    if (position == null) {
+      currentLocationLabel?.remove()
+      currentLocationLabel = null
+      return
+    }
+
+    val point = LatLng.from(position.latitude, position.longitude)
+    currentLocationLabel?.let {
+      it.moveTo(point)
+      return
+    }
+
+    val labelManager = map.labelManager ?: return
+    val layer = labelManager.layer ?: return
+    val styles = labelManager.addLabelStyles(LabelStyles.from(LabelStyle.from(createLocationDot()))) ?: return
+    currentLocationLabel = layer.addLabel(LabelOptions.from(point).setStyles(styles))
+  }
+
+  /** 드로어블 리소스 없이 현재 위치 점을 그린다. 색은 Fishlog 메인 블루(#00A0F0). */
+  private fun createLocationDot(): Bitmap {
+    val density = resources.displayMetrics.density
+    val size = (24 * density).toInt().coerceAtLeast(1)
+    val bitmap = Bitmap.createBitmap(size, size, Bitmap.Config.ARGB_8888)
+    val canvas = Canvas(bitmap)
+    val center = size / 2f
+    val paint = Paint(Paint.ANTI_ALIAS_FLAG)
+
+    paint.color = Color.argb(56, 0, 160, 240)
+    canvas.drawCircle(center, center, center, paint)
+    paint.color = Color.WHITE
+    canvas.drawCircle(center, center, 7f * density, paint)
+    paint.color = Color.rgb(0, 160, 240)
+    canvas.drawCircle(center, center, 5f * density, paint)
+
+    return bitmap
+  }
+
+  private fun moveCamera(value: CameraProps) {
+    val update = CameraUpdateFactory.newCenterPosition(LatLng.from(value.latitude, value.longitude), value.zoomLevel)
+    kakaoMap?.let { map ->
+      if (cameraAnimationDuration > 0) map.moveCamera(update, CameraAnimation.from(cameraAnimationDuration))
+      else map.moveCamera(update)
+    }
+  }
+
+  private fun applyMapOptions(map: KakaoMap) {
+    map.setPoiVisible(poiEnabled)
+    map.setPoiClickable(poiClickable)
+    map.setPoiLanguage(language)
+    map.setPoiScale(poiScale)
+    map.changeMapType(mapType)
+    map.setBuildingHeightScale(buildingScale)
+    map.setCameraMinLevel(cameraMinLevel)
+    map.setCameraMaxLevel(cameraMaxLevel)
+    if (showScaleBar) map.scaleBar?.show() else map.scaleBar?.hide()
+    if (showCompass) map.compass?.show() else map.compass?.hide()
+  }
+
+  override fun onHostResume() = nativeMapView.resume()
+  override fun onHostPause() = nativeMapView.pause()
+  override fun onHostDestroy() = destroy()
+
+  fun destroy() {
+    reactContext.removeLifecycleEventListener(this)
+    nativeMapView.finish()
+    currentLocationLabel = null
+    spotLayer = null
+    spotStyles = null
+    kakaoMap = null
+  }
+}
+
+private const val SPOT_LAYER_ID = "fishlog-spots"
+private const val SPOT_STYLE_ID = "fishlog-spot-pin"
diff --git a/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapViewManager.kt b/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapViewManager.kt
new file mode 100644
index 0000000..b031f64
--- /dev/null
+++ b/node_modules/@react-native-kakao/map/android/src/main/java/net/mjstudio/rnkakao/map/RNCKakaoMapViewManager.kt
@@ -0,0 +1,69 @@
+﻿package net.mjstudio.rnkakao.map
+
+import com.facebook.react.bridge.ReadableArray
+import com.facebook.react.bridge.ReadableMap
+import com.facebook.react.uimanager.BaseViewManagerDelegate
+import com.facebook.react.uimanager.ThemedReactContext
+import com.facebook.react.uimanager.ViewManagerDelegate
+import com.facebook.react.uimanager.SimpleViewManager
+import com.facebook.react.viewmanagers.RNCKakaoMapViewManagerDelegate
+import com.facebook.react.viewmanagers.RNCKakaoMapViewManagerInterface
+import com.kakao.vectormap.MapType
+import com.kakao.vectormap.PoiScale
+
+internal class RNCKakaoMapViewManager : SimpleViewManager<RNCKakaoMapView>(), RNCKakaoMapViewManagerInterface<RNCKakaoMapView> {
+  private val delegate: ViewManagerDelegate<RNCKakaoMapView> = RNCKakaoMapViewManagerDelegate(this)
+
+  override fun getName() = "RNCKakaoMapView"
+  override fun getDelegate(): ViewManagerDelegate<RNCKakaoMapView> = delegate
+  override fun createViewInstance(context: ThemedReactContext) = RNCKakaoMapView(context)
+  override fun onDropViewInstance(view: RNCKakaoMapView) { view.destroy(); super.onDropViewInstance(view) }
+
+  /** onSpotPress 는 뷰 자신에게만 올라가는 direct event 다. */
+  override fun getExportedCustomDirectEventTypeConstants(): MutableMap<String, Any> =
+    mutableMapOf(SpotPressEvent.EVENT_NAME to mutableMapOf("registrationName" to "onSpotPress"))
+
+  override fun setBuildingScale(view: RNCKakaoMapView, value: Double) { view.buildingScale = value.toFloat() }
+  override fun setOverlays(view: RNCKakaoMapView, value: ReadableArray?) = Unit
+  override fun setPoiEnabled(view: RNCKakaoMapView, value: Boolean) { view.poiEnabled = value }
+  override fun setPoiClickable(view: RNCKakaoMapView, value: Boolean) { view.poiClickable = value }
+  override fun setPoiScale(view: RNCKakaoMapView, value: String?) { view.poiScale = runCatching { PoiScale.valueOf(value?.uppercase() ?: "REGULAR") }.getOrDefault(PoiScale.REGULAR) }
+  override fun setLanguage(view: RNCKakaoMapView, value: String?) { view.language = value ?: "ko" }
+  override fun setBaseMapType(view: RNCKakaoMapView, value: String?) { view.mapType = if (value.equals("skyview", true)) MapType.SKYVIEW else MapType.NORMAL }
+  override fun setCamera(view: RNCKakaoMapView, value: ReadableMap?) { value?.toCamera()?.let(view::setCamera) }
+  override fun setCurrentLocation(view: RNCKakaoMapView, value: ReadableMap?) { view.setCurrentLocation(value?.toLatLng()) }
+  override fun setSpots(view: RNCKakaoMapView, value: ReadableArray?) { view.setSpots(value?.toSpots() ?: emptyList()) }
+  override fun setInitialCamera(view: RNCKakaoMapView, value: ReadableMap?) { value?.toCamera()?.let(view::setCamera) }
+  override fun setCameraMinLevel(view: RNCKakaoMapView, value: Double) { view.cameraMinLevel = value.toInt() }
+  override fun setCameraMaxLevel(view: RNCKakaoMapView, value: Double) { view.cameraMaxLevel = value.toInt() }
+  override fun setCameraAnimationDuration(view: RNCKakaoMapView, value: Double) { view.cameraAnimationDuration = value.toInt() }
+  override fun setIsShowScaleBar(view: RNCKakaoMapView, value: Boolean) { view.showScaleBar = value }
+  override fun setIsShowCompass(view: RNCKakaoMapView, value: Boolean) { view.showCompass = value }
+
+  private fun ReadableArray.toSpots(): List<SpotProps> {
+    val result = mutableListOf<SpotProps>()
+    for (index in 0 until size()) {
+      val item = getMap(index) ?: continue
+      if (!item.hasKey("id") || !item.hasKey("lat") || !item.hasKey("lng")) continue
+      result.add(SpotProps(item.getDouble("id").toInt(), item.getDouble("lat"), item.getDouble("lng")))
+    }
+    return result
+  }
+
+  private fun ReadableMap.toLatLng(): LatLngProps? {
+    if (!hasKey("lat") || !hasKey("lng")) return null
+    val lat = getDouble("lat")
+    val lng = getDouble("lng")
+    if (lat == NULL_NUMBER || lng == NULL_NUMBER) return null
+    return LatLngProps(lat, lng)
+  }
+
+  private fun ReadableMap.toCamera() = CameraProps(
+    latitude = getDouble("lat"),
+    longitude = getDouble("lng"),
+    zoomLevel = if (hasKey("zoomLevel")) getDouble("zoomLevel").toInt() else 15,
+  )
+}
+
+/** JS util/Const.ts 의 nullNumber. codegen 이 값을 채우지 않은 필드를 뜻한다. */
+private const val NULL_NUMBER = -123123123.0
diff --git a/node_modules/@react-native-kakao/map/lib/commonjs/component/KakaoMapView.js b/node_modules/@react-native-kakao/map/lib/commonjs/component/KakaoMapView.js
index e7fbf55..96f48e2 100644
--- a/node_modules/@react-native-kakao/map/lib/commonjs/component/KakaoMapView.js
+++ b/node_modules/@react-native-kakao/map/lib/commonjs/component/KakaoMapView.js
@@ -20,6 +20,7 @@ const KakaoMapView = exports.KakaoMapView = /*#__PURE__*/(0, _react.forwardRef)(
   language,
   overlays,
   camera: cameraProp,
+  currentLocation: currentLocationProp,
   cameraMinLevel = _Const.Const.nullNumber,
   cameraMaxLevel = _Const.Const.nullNumber,
   cameraAnimationDuration = 0,
@@ -35,10 +36,17 @@ const KakaoMapView = exports.KakaoMapView = /*#__PURE__*/(0, _react.forwardRef)(
       ...cameraProp
     };
   }, [cameraProp]);
+  const currentLocation = (0, _react.useMemo)(() => {
+    return {
+      ..._Util.nullCamera,
+      ...currentLocationProp
+    };
+  }, [currentLocationProp]);
   return /*#__PURE__*/React.createElement(_RNCKakaoMapViewNativeComponent.default, _extends({
     baseMapType: baseMapType,
     buildingScale: buildingScale,
     camera: camera,
+    currentLocation: currentLocation,
     cameraMinLevel: cameraMinLevel,
     cameraMaxLevel: cameraMaxLevel,
     cameraAnimationDuration: cameraAnimationDuration,
diff --git a/node_modules/@react-native-kakao/map/lib/commonjs/util/Util.js b/node_modules/@react-native-kakao/map/lib/commonjs/util/Util.js
index ca954f2..a56096d 100644
--- a/node_modules/@react-native-kakao/map/lib/commonjs/util/Util.js
+++ b/node_modules/@react-native-kakao/map/lib/commonjs/util/Util.js
@@ -10,6 +10,7 @@ const nullCamera = exports.nullCamera = {
   lng: _Const.Const.nullNumber,
   tilt: _Const.Const.nullNumber,
   rotation: _Const.Const.nullNumber,
-  zoomLevel: _Const.Const.nullNumber
+  zoomLevel: _Const.Const.nullNumber,
+  nonce: _Const.Const.nullNumber
 };
 //# sourceMappingURL=Util.js.map
\ No newline at end of file
diff --git a/node_modules/@react-native-kakao/map/lib/module/component/KakaoMapView.js b/node_modules/@react-native-kakao/map/lib/module/component/KakaoMapView.js
index 3c1776b..fe397cd 100644
--- a/node_modules/@react-native-kakao/map/lib/module/component/KakaoMapView.js
+++ b/node_modules/@react-native-kakao/map/lib/module/component/KakaoMapView.js
@@ -13,6 +13,7 @@ const KakaoMapView = /*#__PURE__*/forwardRef(({
   language,
   overlays,
   camera: cameraProp,
+  currentLocation: currentLocationProp,
   cameraMinLevel = Const.nullNumber,
   cameraMaxLevel = Const.nullNumber,
   cameraAnimationDuration = 0,
@@ -28,10 +29,17 @@ const KakaoMapView = /*#__PURE__*/forwardRef(({
       ...cameraProp
     };
   }, [cameraProp]);
+  const currentLocation = useMemo(() => {
+    return {
+      ...nullCamera,
+      ...currentLocationProp
+    };
+  }, [currentLocationProp]);
   return /*#__PURE__*/React.createElement(NativeKakaoMapComponent, _extends({
     baseMapType: baseMapType,
     buildingScale: buildingScale,
     camera: camera,
+    currentLocation: currentLocation,
     cameraMinLevel: cameraMinLevel,
     cameraMaxLevel: cameraMaxLevel,
     cameraAnimationDuration: cameraAnimationDuration,
diff --git a/node_modules/@react-native-kakao/map/lib/module/util/Util.js b/node_modules/@react-native-kakao/map/lib/module/util/Util.js
index 6a6cd98..1f79d7a 100644
--- a/node_modules/@react-native-kakao/map/lib/module/util/Util.js
+++ b/node_modules/@react-native-kakao/map/lib/module/util/Util.js
@@ -4,6 +4,7 @@ export const nullCamera = {
   lng: Const.nullNumber,
   tilt: Const.nullNumber,
   rotation: Const.nullNumber,
-  zoomLevel: Const.nullNumber
+  zoomLevel: Const.nullNumber,
+  nonce: Const.nullNumber
 };
 //# sourceMappingURL=Util.js.map
\ No newline at end of file
diff --git a/node_modules/@react-native-kakao/map/lib/typescript/src/component/KakaoMapView.d.ts b/node_modules/@react-native-kakao/map/lib/typescript/src/component/KakaoMapView.d.ts
index e6cacbe..e4d3a2d 100644
--- a/node_modules/@react-native-kakao/map/lib/typescript/src/component/KakaoMapView.d.ts
+++ b/node_modules/@react-native-kakao/map/lib/typescript/src/component/KakaoMapView.d.ts
@@ -6,6 +6,10 @@ interface KakaoMapProps extends ViewProps {
     overlays?: ('hill_shading' | 'roadview_line' | 'bicycle_road' | 'hybrid')[];
     camera?: Camera;
     initialCamera?: Camera;
+    currentLocation?: {
+        lat: number;
+        lng: number;
+    };
     cameraMinLevel?: number;
     cameraMaxLevel?: number;
     cameraAnimationDuration?: number;
@@ -16,6 +20,16 @@ interface KakaoMapProps extends ViewProps {
     language?: 'ko' | 'en';
     isShowScaleBar?: boolean;
     isShowCompass?: boolean;
+    spots?: ReadonlyArray<{
+        id: number;
+        lat: number;
+        lng: number;
+    }>;
+    onSpotPress?: (event: {
+        nativeEvent: {
+            id: number;
+        };
+    }) => void;
 }
 interface KakaoMapRef {
 }
diff --git a/node_modules/@react-native-kakao/map/lib/typescript/src/spec/RNCKakaoMapViewNativeComponent.d.ts b/node_modules/@react-native-kakao/map/lib/typescript/src/spec/RNCKakaoMapViewNativeComponent.d.ts
index af0146e..9afbf12 100644
--- a/node_modules/@react-native-kakao/map/lib/typescript/src/spec/RNCKakaoMapViewNativeComponent.d.ts
+++ b/node_modules/@react-native-kakao/map/lib/typescript/src/spec/RNCKakaoMapViewNativeComponent.d.ts
@@ -7,6 +7,8 @@ interface Camera {
     tilt?: Double;
     rotation?: Double;
     zoomLevel?: Double;
+    /** 같은 좌표로 다시 카메라를 옮길 때 쓰는 갱신 토큰. */
+    nonce?: Double;
 }
 interface Props extends ViewProps {
     buildingScale?: Double;
@@ -18,6 +20,7 @@ interface Props extends ViewProps {
     baseMapType?: string;
     camera?: Camera;
     initialCamera?: Camera;
+    currentLocation?: Camera;
     cameraMinLevel?: Double;
     cameraMaxLevel?: Double;
     cameraAnimationDuration?: Double;
diff --git a/node_modules/@react-native-kakao/map/lib/typescript/src/type/type.d.ts b/node_modules/@react-native-kakao/map/lib/typescript/src/type/type.d.ts
index f598be9..279517c 100644
--- a/node_modules/@react-native-kakao/map/lib/typescript/src/type/type.d.ts
+++ b/node_modules/@react-native-kakao/map/lib/typescript/src/type/type.d.ts
@@ -4,5 +4,7 @@ export interface Camera {
     tilt?: number;
     rotation?: number;
     zoomLevel?: number;
+    /** 같은 좌표로 다시 카메라를 옮길 때 쓰는 갱신 토큰. */
+    nonce?: number;
 }
 //# sourceMappingURL=type.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@react-native-kakao/map/src/component/KakaoMapView.tsx b/node_modules/@react-native-kakao/map/src/component/KakaoMapView.tsx
index 53c719e..da62ccc 100644
--- a/node_modules/@react-native-kakao/map/src/component/KakaoMapView.tsx
+++ b/node_modules/@react-native-kakao/map/src/component/KakaoMapView.tsx
@@ -12,6 +12,8 @@ interface KakaoMapProps extends ViewProps {
   overlays?: ('hill_shading' | 'roadview_line' | 'bicycle_road' | 'hybrid')[];
   camera?: Camera;
   initialCamera?: Camera;
+  /** 현재 위치 표시용 좌표. 없으면 마커를 지운다. */
+  currentLocation?: { lat: number; lng: number };
   cameraMinLevel?: number;
   cameraMaxLevel?: number;
   cameraAnimationDuration?: number;
@@ -24,6 +26,11 @@ interface KakaoMapProps extends ViewProps {
 
   isShowScaleBar?: boolean;
   isShowCompass?: boolean;
+
+  /** 지도에 찍을 낚시 스팟 마커 */
+  spots?: ReadonlyArray<{ id: number; lat: number; lng: number }>;
+  /** 마커를 눌렀을 때. nativeEvent.id 가 스팟 id 다 */
+  onSpotPress?: (event: { nativeEvent: { id: number } }) => void;
 }
 interface KakaoMapRef {}
 
@@ -38,6 +45,7 @@ const KakaoMapView = forwardRef(
       language,
       overlays,
       camera: cameraProp,
+      currentLocation: currentLocationProp,
       cameraMinLevel = Const.nullNumber,
       cameraMaxLevel = Const.nullNumber,
       cameraAnimationDuration = 0,
@@ -57,11 +65,16 @@ const KakaoMapView = forwardRef(
       return { ...nullCamera, ...cameraProp };
     }, [cameraProp]);
 
+    const currentLocation: Camera = useMemo(() => {
+      return { ...nullCamera, ...currentLocationProp };
+    }, [currentLocationProp]);
+
     return (
       <NativeKakaoMapComponent
         baseMapType={baseMapType}
         buildingScale={buildingScale}
         camera={camera}
+        currentLocation={currentLocation}
         cameraMinLevel={cameraMinLevel}
         cameraMaxLevel={cameraMaxLevel}
         cameraAnimationDuration={cameraAnimationDuration}
diff --git a/node_modules/@react-native-kakao/map/src/index.ts b/node_modules/@react-native-kakao/map/src/index.ts
index 1cc6a4d..e143fee 100644
--- a/node_modules/@react-native-kakao/map/src/index.ts
+++ b/node_modules/@react-native-kakao/map/src/index.ts
@@ -10,12 +10,6 @@ const LINKING_ERROR =
   '- You rebuilt the app after installing the package\n' +
   '- You are not using Expo Go\n';
 
-const isTurboModuleEnabled = global.__turboModuleProxy != null;
-
-if (!isTurboModuleEnabled) {
-  throw new Error('The package @react-native-kakao/map only supports fabric');
-}
-
 const Module = require('./spec/NativeKakaoMap').default;
 
 const Native: Spec = Module
diff --git a/node_modules/@react-native-kakao/map/src/spec/RNCKakaoMapViewNativeComponent.ts b/node_modules/@react-native-kakao/map/src/spec/RNCKakaoMapViewNativeComponent.ts
index 85a6f84..c430214 100644
--- a/node_modules/@react-native-kakao/map/src/spec/RNCKakaoMapViewNativeComponent.ts
+++ b/node_modules/@react-native-kakao/map/src/spec/RNCKakaoMapViewNativeComponent.ts
@@ -1,5 +1,8 @@
 import type { ViewProps } from 'react-native';
-import type { Double } from 'react-native/Libraries/Types/CodegenTypes';
+import type {
+  DirectEventHandler,
+  Double,
+} from 'react-native/Libraries/Types/CodegenTypes';
 import codegenNativeCommands from 'react-native/Libraries/Utilities/codegenNativeCommands';
 import codegenNativeComponent from 'react-native/Libraries/Utilities/codegenNativeComponent';
 
@@ -9,7 +12,22 @@ interface Camera {
   tilt?: Double;
   rotation?: Double;
   zoomLevel?: Double;
+  /** 같은 좌표로 다시 카메라를 옮길 때 쓰는 갱신 토큰. Fabric 은 prop 을 값으로 비교하므로
+   *  값이 그대로면 네이티브까지 전달되지 않는다. 사용자가 지도를 옮긴 뒤 원래 좌표로
+   *  되돌리려면 이 값을 바꿔야 한다. 네이티브는 값 자체를 쓰지 않는다. */
+  nonce?: Double;
 }
+/** 지도에 찍을 낚시 스팟 마커 한 개 */
+interface SpotMarker {
+  id: Double;
+  lat: Double;
+  lng: Double;
+}
+
+interface SpotPressEvent {
+  id: Double;
+}
+
 interface Props extends ViewProps {
   buildingScale?: Double;
   overlays?: ReadonlyArray<string>;
@@ -20,12 +38,16 @@ interface Props extends ViewProps {
   baseMapType?: string;
   camera?: Camera;
   initialCamera?: Camera;
+  currentLocation?: Camera;
   cameraMinLevel?: Double;
   cameraMaxLevel?: Double;
   cameraAnimationDuration?: Double;
 
   isShowScaleBar?: boolean;
   isShowCompass?: boolean;
+
+  spots?: ReadonlyArray<SpotMarker>;
+  onSpotPress?: DirectEventHandler<SpotPressEvent>;
 }
 
 // type ComponentType = HostComponent<Props>;
diff --git a/node_modules/@react-native-kakao/map/src/type/type.ts b/node_modules/@react-native-kakao/map/src/type/type.ts
index bd55a4d..4ad0604 100644
--- a/node_modules/@react-native-kakao/map/src/type/type.ts
+++ b/node_modules/@react-native-kakao/map/src/type/type.ts
@@ -4,4 +4,8 @@ export interface Camera {
   tilt?: number;
   rotation?: number;
   zoomLevel?: number;
+  /** 같은 좌표로 다시 카메라를 옮길 때 쓰는 갱신 토큰. Fabric 은 prop 을 값으로 비교하므로
+   *  값이 그대로면 네이티브까지 전달되지 않는다. 사용자가 지도를 옮긴 뒤 원래 좌표로
+   *  되돌리려면 이 값을 바꿔야 한다. 네이티브는 값 자체를 쓰지 않는다. */
+  nonce?: number;
 }
diff --git a/node_modules/@react-native-kakao/map/src/util/Util.ts b/node_modules/@react-native-kakao/map/src/util/Util.ts
index 2234ec4..870546c 100644
--- a/node_modules/@react-native-kakao/map/src/util/Util.ts
+++ b/node_modules/@react-native-kakao/map/src/util/Util.ts
@@ -8,4 +8,5 @@ export const nullCamera: Required<Camera> = {
   tilt: Const.nullNumber,
   rotation: Const.nullNumber,
   zoomLevel: Const.nullNumber,
+  nonce: Const.nullNumber,
 };
